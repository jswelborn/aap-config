---
controller_credentials:
  - name: GitHub PAT - AAP-cac
    description: GitHub personal access token used for AAP project source control synchronization
    organization: HTS SRE
    credential_type: Source Control
    inputs:
      username: "{{ github_username }}"
      password: "{{ github_pat }}"