---
- name: Configure Linux job templates in AAP
  hosts: localhost
  connection: localhost
  gather_facts: false

  vars_files:
    - ../job_templates/linux.yaml
    - ../group_vars/all.yaml

  roles:
    - infra.aap_configuration.controller_job_templates