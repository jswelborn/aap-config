---
- name: Configure platform job templates in AAP
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../job_templates/platform.yaml
    - ../group_vars/all.yaml

  roles:
    - infra.aap_configuration.controller_job_templates