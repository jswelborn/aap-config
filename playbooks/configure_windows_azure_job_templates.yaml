---
- name: Configure Windows Azure job templates in AAP
  hosts: localhost
  connection: localhost
  gather_facts: false

  vars_files:
    - ../job_templates/windows_azure.yaml
    - ../group_vars/all.yaml

  roles:
    - infra.aap_configuration.controller_job_templates